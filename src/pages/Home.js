import React from 'react';
import Header from '../components/Header';

const Home = (props) => {
    return (
        // <div>Home</div>
        <React.Fragment>
            <Header />
        </React.Fragment>
    );
};

export default Home;